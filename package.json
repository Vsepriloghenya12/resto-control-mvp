{
  "name": "resto-control-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "SaaS MVP для ресторанов: чек-листы, заявки, инвентаризация, задачи и база знаний",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace webapp\"",
    "build": "npm run build --workspace webapp",
    "start": "node server/index.js",
    "reset-db": "node server/scripts/reset-db.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "server",
    "webapp"
  ]
}
